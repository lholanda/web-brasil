pipeline {
    agent any

    triggers {
        pollSCM('* * * * *')
    }

    stages {
        stage ('Build Image') {
            steps {
                script {
                    echo 'build image ****'

                    /*dockerapp= docker.build("lholanda/web-brasil:teste", "-f ./src/Dockerfile ./src")*/

                }
            }
        }
        stage ('Push Image') {
            steps {
                script {
                    echo 'push image ****'

                    /*dockerapp= docker.build("lholanda/web-brasil:teste", "-f ./src/Dockerfile ./src")*/

                }
            }
        }
    }    
}